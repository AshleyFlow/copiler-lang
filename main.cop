let workspace = game.GetService(game, "Workspace")

let get_baseplate = () {
    let baseplate = workspace.FindFirstChild(workspace, "Basepart")

    if baseplate {
        return baseplate
    }

    error("Couldn't find workspace")
}

let baseplate_exists = get_baseplate() && true || false
print(baseplate_exists)
