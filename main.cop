let workspace = game.GetService(game, "Workspace")

let get_baseplate = () {
    let baseplate = workspace.FindFirstChild(workspace, "Basepart")
    return baseplate
}

let baseplate = get_baseplate()
let baseplate_exists = baseplate && true || false

print(baseplate, baseplate_exists)

baseplate.Name = "Ruin"

task.wait(5)

baseplate.Destroy(baseplate)
