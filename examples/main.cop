class Car {
    let speed = 0
    
    let change_speed = (new: number) {
        self.speed = new
    }
}

let new_car = Car.new()
new_car.change_speed(new_car, 10)

task.wait(1)

new_car.change_speed(new_car, 0)
